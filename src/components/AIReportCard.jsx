import React from 'react';
import { Sparkles, TrendingUp, Award, Target } from 'lucide-react';

const AIReportCard = ({ report, loading }) => {
    if (loading) {
        return (
            <div className="glass-card" style={{ padding: '24px', marginBottom: '24px', textAlign: 'center' }}>
                <Sparkles className="spin" size={32} style={{ color: 'var(--accent-primary)', marginBottom: '16px' }} />
                <h3 style={{ color: 'var(--text-primary)' }}>Generating Your Weekly Report...</h3>
                <p style={{ color: 'var(--text-secondary)', marginTop: '8px' }}>
                    Analyzing your habits and finances to give you personalized insights.
                </p>
                <style>{`
                    .spin { animation: spin 2s linear infinite; }
                    @keyframes spin { 100% { transform: rotate(360deg); } }
                `}</style>
            </div>
        );
    }

    if (!report) return null;

    return (
        <div className="glass-card" style={{ padding: '24px', marginBottom: '24px', position: 'relative', overflow: 'hidden' }}>
            {/* Accent top bar */}
            <div style={{
                position: 'absolute', top: 0, left: 0, right: 0, height: '4px',
                background: 'var(--accent-gradient)',
            }} />

            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'flex-start', marginBottom: '20px' }}>
                <div>
                    <h2 style={{ fontSize: '20px', fontWeight: '700', display: 'flex', alignItems: 'center', gap: '8px', color: 'var(--text-primary)' }}>
                        <Sparkles size={20} style={{ color: 'var(--accent-primary)' }} />
                        Weekly Insights
                    </h2>
                    <p style={{ fontSize: '13px', color: 'var(--text-muted)', marginTop: '4px' }}>
                        Generated by AI • {new Date().toLocaleDateString()}
                    </p>
                </div>
                <div style={{
                    background: 'var(--glass-card-bg)',
                    border: '1px solid var(--glass-card-border)',
                    color: 'var(--accent-primary)',
                    padding: '8px 16px',
                    borderRadius: '20px',
                    fontWeight: '700',
                    fontSize: '24px',
                }}>
                    {report.score}<span style={{ fontSize: '14px' }}>/100</span>
                </div>
            </div>

            <p style={{ fontSize: '15px', lineHeight: '1.6', marginBottom: '24px', fontStyle: 'italic', color: 'var(--text-secondary)' }}>
                "{report.summary}"
            </p>

            <div style={{ display: 'grid', gap: '16px', gridTemplateColumns: 'repeat(auto-fit, minmax(280px, 1fr))' }}>
                <div style={{ background: 'var(--glass-card-bg)', border: '1px solid var(--glass-card-border)', padding: '16px', borderRadius: '12px' }}>
                    <h3 style={{ fontSize: '14px', fontWeight: '600', marginBottom: '12px', display: 'flex', alignItems: 'center', gap: '8px', color: 'var(--text-primary)' }}>
                        <Award size={16} color="#f6ad55" /> Highlights
                    </h3>
                    <ul style={{ paddingLeft: '20px', margin: 0, fontSize: '14px', lineHeight: '1.5', color: 'var(--text-secondary)' }}>
                        {report.highlights?.map((h, i) => <li key={i}>{h}</li>)}
                    </ul>
                </div>

                <div style={{ background: 'var(--glass-card-bg)', border: '1px solid var(--glass-card-border)', padding: '16px', borderRadius: '12px' }}>
                    <h3 style={{ fontSize: '14px', fontWeight: '600', marginBottom: '12px', display: 'flex', alignItems: 'center', gap: '8px', color: 'var(--text-primary)' }}>
                        <TrendingUp size={16} style={{ color: 'var(--success)' }} /> Analysis
                    </h3>
                    <div style={{ display: 'flex', flexDirection: 'column', gap: '8px', fontSize: '14px', color: 'var(--text-secondary)' }}>
                        <p><strong>Spending:</strong> {report.spendingAnalysis}</p>
                        <p><strong>Habits:</strong> {report.habitAnalysis}</p>
                        {report.journalInsight && <p><strong>Mindset:</strong> {report.journalInsight}</p>}
                    </div>
                </div>
            </div>

            <div style={{
                marginTop: '20px',
                padding: '16px',
                background: 'var(--glass-card-bg)',
                border: '1px solid var(--glass-card-border)',
                borderRadius: '12px',
                display: 'flex',
                gap: '12px',
                alignItems: 'center',
            }}>
                <Target size={24} style={{ color: 'var(--accent-primary)', flexShrink: 0 }} />
                <div>
                    <h4 style={{ fontSize: '14px', fontWeight: '700', color: 'var(--accent-primary)', marginBottom: '2px' }}>
                        Focus for Next Week
                    </h4>
                    <p style={{ fontSize: '14px', color: 'var(--text-secondary)' }}>{report.suggestion}</p>
                </div>
            </div>
        </div>
    );
};

export default AIReportCard;
